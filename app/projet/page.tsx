import { ProjectHub } from "@/components/project/ProjectHub";

/** Carte complète du MVP — même contenu qu’à l’accueil en mode développement. */
export default function ProjetPage() {
  return <ProjectHub />;
}
