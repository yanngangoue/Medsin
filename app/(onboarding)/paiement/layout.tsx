/** Layout élargi pour la page paiement (2 colonnes desktop). */
export default function PaiementLayout({ children }: { children: React.ReactNode }) {
  return <div className="mx-auto w-full max-w-6xl">{children}</div>;
}
