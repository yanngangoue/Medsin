import { APP_BRAND } from "@/lib/brand/app-brand";

/** Nom et libellés de l'assistante santé IA (propulsée par Claude côté serveur). */
export const COACH_NAME = APP_BRAND.coachName;

export const COACH_TITLE = `${COACH_NAME} — coach santé`;

export const COACH_INTRO = `Je suis ${COACH_NAME}, votre coach santé IA sur ${APP_BRAND.name}. Je ne remplace pas votre IPS ni un professionnel de santé.`;
